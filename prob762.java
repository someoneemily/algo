prob762
class Solution {
    public int countPrimeSetBits(int L, int R) {
       //start at lowest number, can then find which positions you can put a 1
        // at the highest number, the 1's can't go above that.
    }
}
